<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Half Caf</title>
    <link rel="stylesheet" href="/css/pico.min.css" />
    <link rel="stylesheet" href="/css/styles.css" />
    <link rel="shortcut icon" type="image/png" href="/img/logo.png" />
    <style>
      @media screen and (max-width: 768px) {
        .full-title {
          display: none;
        }
      }
      @media screen and (min-width: 769px) {
        .mobile-title {
          display: none;
        }
        /* Updated arrow trigger styling */
        #leftTrigger {
          position: fixed;
          top: 50%;
          left: 10px; /* start at collapsed sidebar's edge */
          transform: translateY(-50%);
          width: 30px;
          height: 30px;
          background: var(--coffee-medium);
          cursor: pointer;
          z-index: 1003;
          transition: left 0.3s ease;
          opacity: 0.8; /* added transparency */
          border-top-right-radius: 4px; /* round top right corner */
          border-bottom-right-radius: 4px; /* round bottom right corner */
        }
        #leftTrigger span {
          display: block;
          text-align: center;
          color: white;
          line-height: 30px;
        }
        /* On sidebar hover, move arrow to extended position */
        #sidebar:hover + #leftTrigger {
          left: 200px;
        }
      }
    </style>
  </head>
  <body>
    <div class="container" id="logo">
      <%- include('_header') %>
      <h1 class="indexTopBar">
        <span class="full-title">NNHS Half Caf</span>
        <span class="mobile-title">Half Caf</span>
      </h1>
    </div>
    <!-- Reorder: Place sidebar first, then arrow trigger -->
    <div id="sidebar">
      <a href="/homePopularDrinks">Popular Drinks</a>
      <a href="/homeMenu">Menu</a>
      <a href="/auth">Login</a>
    </div>
    <div id="leftTrigger"><span>&rarr;</span></div>
  </body>
</html>
